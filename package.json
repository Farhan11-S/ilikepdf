{
  "name": "ilikepdf",
  "private": true,
  "version": "0.1.0",
  "description": "Client-side PDF tools. The site itself needs no install — this file exists only for the browser smoke tests.",
  "type": "module",
  "scripts": {
    "serve": "python3 -m http.server 8000",
    "test": "node tests/home.smoke.mjs && node tests/merge.smoke.mjs && node tests/split.smoke.mjs"
  },
  "devDependencies": {
    "playwright-core": "^1.56.1"
  }
}
